Você está trabalhando no repositório frontend dashboard-memorial.

Você deve implementar exatamente UMA user story do PRD, com mudanças pequenas, seguras e revisáveis.

PRD completo:
{
  "project": "api-memorial-descritivo",
  "iteration_name": "production-readiness-healthchecks",
  "max_iterations_per_story": 3,
  "global_constraints": [
    "Trabalhar somente no repositório api-memorial-descritivo.",
    "Não alterar o frontend.",
    "Não alterar contratos existentes da API.",
    "Não alterar endpoints existentes de geração, upload, sessões ou download, salvo se for estritamente necessário e sem quebrar compatibilidade.",
    "Não adicionar dependências novas sem necessidade real.",
    "Não expor secrets, tokens, URLs com credenciais, service keys, senhas ou variáveis sensíveis em respostas HTTP.",
    "Fazer a menor alteração segura possível.",
    "Criar ou atualizar testes automatizados.",
    "Rodar os testes disponíveis antes de finalizar.",
    "Usar backend_readiness_notes.md como contexto principal."
  ],
  "user_stories": [
    {
      "id": "RDY-001",
      "title": "Adicionar healthcheck e readiness check de produção para a API",
      "status": "todo",
      "context": "A API está deployada no Railway para testes básicos, mas ainda precisa de uma base mínima de production readiness. O Railway pode usar um endpoint de healthcheck para ativar um novo deploy somente quando a aplicação responder com sucesso. A API precisa separar processo vivo de aplicação pronta para receber tráfego.",
      "problem": "Sem endpoints explícitos de liveness e readiness, fica difícil validar deploy, diagnosticar falhas iniciais e configurar o Railway de forma segura.",
      "source_files_to_review_first": [
        "backend_readiness_notes.md",
        "app/main.py",
        "app/api/routes.py",
        "app/services",
        "app/schemas",
        "tests",
        "requirements.txt",
        "pyproject.toml",
        "railway.json",
        "Procfile",
        "Dockerfile"
      ],
      "scope_in": [
        "Adicionar endpoint GET /health/live.",
        "Adicionar endpoint GET /health/ready.",
        "O endpoint /health/live deve retornar 200 se o processo FastAPI estiver vivo.",
        "O endpoint /health/live não deve consultar banco, Supabase, filesystem pesado, rede externa ou serviços de terceiros.",
        "O endpoint /health/ready deve validar dependências críticas para a API operar.",
        "O endpoint /health/ready deve retornar 200 quando todas as dependências críticas estiverem disponíveis.",
        "O endpoint /health/ready deve retornar 503 quando alguma dependência crítica falhar.",
        "As respostas devem conter status geral, timestamp e lista de checks com status seguro.",
        "Os checks devem ter nomes claros, como app, templates, storage ou database, conforme fizer sentido no projeto real.",
        "O readiness check deve validar a existência dos templates/schemas necessários para os memoriais suportados, sem hardcoding excessivo se já existir estrutura melhor no projeto.",
        "O readiness check deve validar diretórios locais necessários para escrita temporária ou geração, se isso fizer parte da arquitetura atual.",
        "O readiness check deve validar configuração obrigatória da aplicação, mas sem expor valores sensíveis.",
        "Se Supabase for obrigatório na configuração atual, criar check seguro e testável para Supabase.",
        "Se Supabase não for obrigatório em ambiente local/teste, o check deve refletir isso sem quebrar execução local.",
        "Usar timeout curto em checks que possam bloquear.",
        "Evitar chamadas externas reais nos testes.",
        "Criar testes para /health/live retornando 200.",
        "Criar testes para /health/ready retornando 200 quando checks passam.",
        "Criar testes para /health/ready retornando 503 quando um check crítico falha.",
        "Criar teste garantindo que respostas de health não exponham secrets conhecidos ou valores de variáveis sensíveis.",
        "Documentar em arquivo curto como configurar o Healthcheck Path no Railway para /health/ready.",
        "Se já existir railway.json, avaliar com cuidado se vale configurar healthcheckPath nele.",
        "Se não existir configuração Railway como código, não criar uma configuração invasiva; preferir documentação manual."
      ],
      "scope_out": [
        "Não implementar autenticação nesta iteração.",
        "Não implementar observabilidade completa.",
        "Não implementar OpenTelemetry, Sentry, Datadog ou APM nesta iteração.",
        "Não alterar o fluxo de geração de memorial.",
        "Não alterar templates DOCX.",
        "Não alterar schema de banco.",
        "Não alterar migrations.",
        "Não alterar Supabase em produção.",
        "Não modificar o dashboard.",
        "Não criar nova arquitetura de configuração se já houver uma abordagem simples suficiente.",
        "Não adicionar Dockerfile se o projeto não usa Dockerfile atualmente.",
        "Não transformar essa iteração em uma auditoria geral de segurança."
      ],
      "acceptance_criteria": [
        "GET /health/live existe e retorna HTTP 200 em execução normal.",
        "GET /health/live retorna JSON seguro e simples.",
        "GET /health/live não depende de rede externa, banco, Supabase ou templates.",
        "GET /health/ready existe.",
        "GET /health/ready retorna HTTP 200 quando os checks críticos passam.",
        "GET /health/ready retorna HTTP 503 quando algum check crítico falha.",
        "GET /health/ready identifica qual check falhou por nome, sem expor segredo ou dado sensível.",
        "Os checks são implementados de forma testável, preferencialmente isolados em função ou módulo próprio.",
        "Os testes não dependem de rede externa real.",
        "Os testes cobrem live saudável, ready saudável e ready com falha.",
        "Existe teste ou validação automatizada garantindo que secrets não aparecem na resposta.",
        "A documentação explica que no Railway o Healthcheck Path recomendado é /health/ready.",
        "A documentação explica que /health/live serve para verificar processo vivo, não readiness real.",
        "pytest passa.",
        "Lint/typecheck passam se já forem parte do projeto.",
        "A aplicação continua iniciando localmente."
      ],
      "manual_review_checklist": [
        "Subir a API localmente.",
        "Acessar GET /health/live e confirmar HTTP 200.",
        "Acessar GET /health/ready e confirmar HTTP 200 em ambiente local configurado.",
        "Simular falha de um check crítico via teste automatizado.",
        "Confirmar que /health/ready retorna 503 quando a falha é simulada.",
        "Confirmar que a resposta não mostra SUPABASE_SERVICE_ROLE_KEY, OPENAI_API_KEY, DATABASE_URL com credencial, JWT secret ou qualquer variável sensível.",
        "Confirmar que endpoints antigos continuam funcionando nos testes.",
        "Ler a documentação criada para Railway.",
        "Rodar verify.py."
      ]
    }
  ]
}

User story atual:
{
  "id": "RDY-001",
  "title": "Adicionar healthcheck e readiness check de produção para a API",
  "status": "todo",
  "context": "A API está deployada no Railway para testes básicos, mas ainda precisa de uma base mínima de production readiness. O Railway pode usar um endpoint de healthcheck para ativar um novo deploy somente quando a aplicação responder com sucesso. A API precisa separar processo vivo de aplicação pronta para receber tráfego.",
  "problem": "Sem endpoints explícitos de liveness e readiness, fica difícil validar deploy, diagnosticar falhas iniciais e configurar o Railway de forma segura.",
  "source_files_to_review_first": [
    "backend_readiness_notes.md",
    "app/main.py",
    "app/api/routes.py",
    "app/services",
    "app/schemas",
    "tests",
    "requirements.txt",
    "pyproject.toml",
    "railway.json",
    "Procfile",
    "Dockerfile"
  ],
  "scope_in": [
    "Adicionar endpoint GET /health/live.",
    "Adicionar endpoint GET /health/ready.",
    "O endpoint /health/live deve retornar 200 se o processo FastAPI estiver vivo.",
    "O endpoint /health/live não deve consultar banco, Supabase, filesystem pesado, rede externa ou serviços de terceiros.",
    "O endpoint /health/ready deve validar dependências críticas para a API operar.",
    "O endpoint /health/ready deve retornar 200 quando todas as dependências críticas estiverem disponíveis.",
    "O endpoint /health/ready deve retornar 503 quando alguma dependência crítica falhar.",
    "As respostas devem conter status geral, timestamp e lista de checks com status seguro.",
    "Os checks devem ter nomes claros, como app, templates, storage ou database, conforme fizer sentido no projeto real.",
    "O readiness check deve validar a existência dos templates/schemas necessários para os memoriais suportados, sem hardcoding excessivo se já existir estrutura melhor no projeto.",
    "O readiness check deve validar diretórios locais necessários para escrita temporária ou geração, se isso fizer parte da arquitetura atual.",
    "O readiness check deve validar configuração obrigatória da aplicação, mas sem expor valores sensíveis.",
    "Se Supabase for obrigatório na configuração atual, criar check seguro e testável para Supabase.",
    "Se Supabase não for obrigatório em ambiente local/teste, o check deve refletir isso sem quebrar execução local.",
    "Usar timeout curto em checks que possam bloquear.",
    "Evitar chamadas externas reais nos testes.",
    "Criar testes para /health/live retornando 200.",
    "Criar testes para /health/ready retornando 200 quando checks passam.",
    "Criar testes para /health/ready retornando 503 quando um check crítico falha.",
    "Criar teste garantindo que respostas de health não exponham secrets conhecidos ou valores de variáveis sensíveis.",
    "Documentar em arquivo curto como configurar o Healthcheck Path no Railway para /health/ready.",
    "Se já existir railway.json, avaliar com cuidado se vale configurar healthcheckPath nele.",
    "Se não existir configuração Railway como código, não criar uma configuração invasiva; preferir documentação manual."
  ],
  "scope_out": [
    "Não implementar autenticação nesta iteração.",
    "Não implementar observabilidade completa.",
    "Não implementar OpenTelemetry, Sentry, Datadog ou APM nesta iteração.",
    "Não alterar o fluxo de geração de memorial.",
    "Não alterar templates DOCX.",
    "Não alterar schema de banco.",
    "Não alterar migrations.",
    "Não alterar Supabase em produção.",
    "Não modificar o dashboard.",
    "Não criar nova arquitetura de configuração se já houver uma abordagem simples suficiente.",
    "Não adicionar Dockerfile se o projeto não usa Dockerfile atualmente.",
    "Não transformar essa iteração em uma auditoria geral de segurança."
  ],
  "acceptance_criteria": [
    "GET /health/live existe e retorna HTTP 200 em execução normal.",
    "GET /health/live retorna JSON seguro e simples.",
    "GET /health/live não depende de rede externa, banco, Supabase ou templates.",
    "GET /health/ready existe.",
    "GET /health/ready retorna HTTP 200 quando os checks críticos passam.",
    "GET /health/ready retorna HTTP 503 quando algum check crítico falha.",
    "GET /health/ready identifica qual check falhou por nome, sem expor segredo ou dado sensível.",
    "Os checks são implementados de forma testável, preferencialmente isolados em função ou módulo próprio.",
    "Os testes não dependem de rede externa real.",
    "Os testes cobrem live saudável, ready saudável e ready com falha.",
    "Existe teste ou validação automatizada garantindo que secrets não aparecem na resposta.",
    "A documentação explica que no Railway o Healthcheck Path recomendado é /health/ready.",
    "A documentação explica que /health/live serve para verificar processo vivo, não readiness real.",
    "pytest passa.",
    "Lint/typecheck passam se já forem parte do projeto.",
    "A aplicação continua iniciando localmente."
  ],
  "manual_review_checklist": [
    "Subir a API localmente.",
    "Acessar GET /health/live e confirmar HTTP 200.",
    "Acessar GET /health/ready e confirmar HTTP 200 em ambiente local configurado.",
    "Simular falha de um check crítico via teste automatizado.",
    "Confirmar que /health/ready retorna 503 quando a falha é simulada.",
    "Confirmar que a resposta não mostra SUPABASE_SERVICE_ROLE_KEY, OPENAI_API_KEY, DATABASE_URL com credencial, JWT secret ou qualquer variável sensível.",
    "Confirmar que endpoints antigos continuam funcionando nos testes.",
    "Ler a documentação criada para Railway.",
    "Rodar verify.py."
  ]
}

Tentativa atual:
1

Learnings acumulados:
# Learnings

Este arquivo acumula aprendizados entre iterações do Ralph/Codex neste repositório.

## Contexto do projeto

Este repositório contém a API do sistema de geração de memoriais descritivos.

O sistema já está deployado no Railway para testes básicos, mas ainda está passando por uma etapa de preparação para produção.

O frontend fica em outro repositório:

- ~/Projects/dashboard-memorial

Nesta iteração, o Ralph deve trabalhar apenas neste repositório backend:

- ~/Projects/api-memorial-descritivo

## Regras permanentes

- Não alterar o frontend a partir deste repositório.
- Não alterar contratos existentes da API sem necessidade explícita.
- Não quebrar endpoints já usados pelo dashboard.
- Não mexer em templates DOCX sem que a story peça isso diretamente.
- Não alterar migrations ou schema de banco sem necessidade explícita.
- Não expor secrets, tokens, service keys, URLs com credenciais, senhas ou variáveis sensíveis em respostas HTTP.
- Preferir mudanças pequenas, testáveis e revisáveis.
- Sempre criar ou atualizar testes automatizados quando alterar comportamento da API.
- Sempre rodar o verificador do projeto antes de considerar uma iteração concluída.

## Aprendizados vindos do dashboard

O frontend passou por uma rodada de UX focada em:

- histórico de memoriais gerados;
- detalhe da geração;
- navegação mobile;
- loading de estatísticas;
- upload mantendo arquivos após erro;
- acessibilidade básica.

Esses fluxos dependem da estabilidade dos endpoints do backend. Portanto, nesta etapa do backend, qualquer alteração precisa preservar compatibilidade com o dashboard.

## Objetivo da próxima iteração

A próxima iteração deve focar em production readiness mínimo da API.

Prioridades:

1. Criar endpoint de liveness.
2. Criar endpoint de readiness.
3. Separar processo vivo de aplicação pronta para receber tráfego.
4. Validar dependências críticas de forma segura.
5. Não fazer chamadas externas pesadas ou lentas sem timeout.
6. Garantir que falhas de readiness retornem 503.
7. Garantir que respostas de health não exponham informações sensíveis.
8. Documentar como configurar o healthcheck no Railway.

## Diretriz para healthchecks

O endpoint de liveness deve ser simples e rápido.

Ele deve responder se o processo FastAPI está vivo, sem depender de:

- banco;
- Supabase;
- OpenAI;
- filesystem pesado;
- templates;
- rede externa.

O endpoint de readiness deve responder se a aplicação está pronta para receber tráfego.

Ele pode verificar, conforme fizer sentido na estrutura real do projeto:

- configuração obrigatória;
- existência de templates e schemas necessários;
- diretórios locais necessários;
- capacidade básica de inicialização de serviços internos;
- dependências críticas, sem expor valores sensíveis.

## Cuidados com Railway

O Railway pode usar um healthcheck para decidir quando um deploy novo está pronto para ser ativado. Por isso, o endpoint recomendado para deploy deve ser o readiness, não apenas o liveness. O liveness diz que o processo está vivo; o readiness diz que a aplicação está pronta para receber tráfego. 

## Cuidados com testes

Os testes de readiness não devem depender de serviços externos reais.

Para simular falhas, prefira:

- monkeypatch;
- mocks;
- fixtures temporárias;
- alteração controlada de configuração;
- paths temporários.

Os testes devem cobrir pelo menos:

1. /health/live retornando 200.
2. /health/ready retornando 200 quando tudo está ok.
3. /health/ready retornando 503 quando um check crítico falha.
4. resposta de health não expondo secrets conhecidos.

## Restrições desta iteração

Não transformar esta rodada em uma auditoria completa de segurança.

Não implementar:

- autenticação;
- rate limit;
- observabilidade completa;
- Sentry;
- OpenTelemetry;
- sistema de logs estruturados completo;
- refatoração arquitetural ampla;
- mudanças no fluxo de geração.

Esses pontos podem virar iterações futuras.


Regras obrigatórias:
1. Trabalhe somente neste repositório.
2. Não altere o backend.
3. Não altere contratos de API sem necessidade explícita.
4. Não commite nada.
5. Não adicione dependências novas sem justificar.
6. Preserve funcionalidades existentes.
7. Faça uma implementação incremental e revisável.
8. Ao final, rode os comandos de verificação disponíveis, especialmente build, lint, typecheck e testes se existirem.
9. Depois da implementação, explique resumidamente o que mudou, arquivos alterados, riscos restantes e como revisar manualmente.
