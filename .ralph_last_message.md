Implementei a story `RDY-003` com uma mudança pequena e localizada no backend. Em [app/main.py](/home/juca/Projects/api_memorial_descritivo/app/main.py) entrou uma camada global de tratamento para `HTTPException`, `RequestValidationError` e exceções inesperadas, além de middleware simples de `X-Request-ID` e configuração básica de logging. Em [app/api/errors.py](/home/juca/Projects/api_memorial_descritivo/app/api/errors.py) centralizei a montagem do envelope de erro e a sanitização de texto sensível. Em [app/api/routes.py](/home/juca/Projects/api_memorial_descritivo/app/api/routes.py) removi logs de debug/`print` indevidos e troquei todos os retornos `500` que ainda vazavam `str(error)` por resposta genérica segura, mantendo os `400` já existentes para compatibilidade.

Também atualizei [tests/test_api.py](/home/juca/Projects/api_memorial_descritivo/tests/test_api.py) e [tests/test_generated_memorial_api.py](/home/juca/Projects/api_memorial_descritivo/tests/test_generated_memorial_api.py) para cobrir: erro interno seguro, preservação de status em `HTTPException`, validação `422` útil, rota inexistente com envelope previsível, ausência de secrets/stack trace na resposta e `X-Request-ID` nos healthchecks. Documentei o formato em [README.md](/home/juca/Projects/api_memorial_descritivo/README.md).

Validação executada: `python -m unittest tests.test_api tests.test_generated_memorial_api` e `python3 verify.py`. O `verify.py` passou com `263` testes verdes. Risco residual deliberado: mantive os corpos `400` já usados por fluxos do dashboard sem forçar envelope único em tudo, para não quebrar contrato existente; a padronização forte ficou em erros de framework e falhas internas, que eram o ponto de maior risco.