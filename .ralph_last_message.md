Encontrei o escopo real da story: os uploads entram por `/upload`, `/from-files`, `/from-files/persist` e pela criação de sessão em `/sessoes`, todos concentrados em `app/api/routes.py` e `app/services/file_ingestion.py`. Hoje já existe validação de extensão e `content-type`, mas faltam limites explícitos de quantidade/tamanho, rejeição de arquivo vazio e um envelope JSON estável para erros de upload; a limpeza de temporários já existe em alguns caminhos e vale preservar.

Proposta mínima para `RDY-004`:
1. Centralizar em `app/services/file_ingestion.py` uma política única de upload com extensões atuais (`.pdf`, `.docx`), máximo de arquivos, máximo por arquivo, máximo por lote e rejeição de arquivo vazio ainda durante a ingestão.
2. Fazer `FileIngestionError` carregar `code`, `status_code` e `detail`, e usar um helper nas rotas para devolver JSON consistente para o frontend sem mudar os casos de sucesso.
3. Atualizar `learnings.md` de forma append-only com o mapa dos entrypoints de upload e documentar o contrato no `README.md`.
4. Cobrir com testes focados em extensão inválida, arquivo vazio, excesso de quantidade, tamanho acima do limite e um fluxo válido mínimo, além de verificar cleanup em um caminho de sucesso e um de falha que já existem.

Se essa abordagem mínima está correta, eu implemento em seguida sem ampliar escopo.