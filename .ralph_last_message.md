Implementei a story `RDY-002` com uma mudança pequena e concentrada: criei uma camada simples de configuração em [app/config.py](/home/juca/Projects/api_memorial_descritivo/app/config.py), passei o app a ser montado por `create_app()` em [app/main.py](/home/juca/Projects/api_memorial_descritivo/app/main.py), e liguei o readiness a essa configuração em [app/services/health.py](/home/juca/Projects/api_memorial_descritivo/app/services/health.py). Agora a API diferencia `local`, `test` e `production`, usa `CORS_ALLOWED_ORIGINS` com normalização de espaços/entradas vazias, aceita defaults locais seguros em `local/test`, e falha explicitamente ao iniciar em `production` sem origens configuradas. Mantive compatibilidade de transição aceitando `CORS_ORIGINS` como fallback legado se `CORS_ALLOWED_ORIGINS` não estiver definido.

Também atualizei [tests/test_app_config.py](/home/juca/Projects/api_memorial_descritivo/tests/test_app_config.py) para cobrir ambiente local/test, production válida, production inválida no startup, origem permitida e origem não permitida; e documentei as variáveis em [.env.example](/home/juca/Projects/api_memorial_descritivo/.env.example) e [docs/railway-healthchecks.md](/home/juca/Projects/api_memorial_descritivo/docs/railway-healthchecks.md). Não adicionei dependências novas.

Validação executada: `./.venv/bin/python -m pytest tests/test_app_config.py tests/test_generated_memorial_api.py tests/test_api.py` com `51 passed`, e `./.venv/bin/python verify.py` com `258 passed`. Risco restante baixo: como mantive o alias `CORS_ORIGINS` por compatibilidade, ainda existe uma rota de configuração legada; o caminho recomendado agora é usar apenas `APP_ENV` e `CORS_ALLOWED_ORIGINS`. Para revisão manual, vale testar `GET /health/live`, `GET /health/ready`, um `OPTIONS` com origem permitida local e um boot com `APP_ENV=production` sem `CORS_ALLOWED_ORIGINS` para confirmar a falha explícita.